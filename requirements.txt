# Core MCP server + HTTP (SSE) transport
fastmcp>=0.4.0
mcp>=1.2.0
uvicorn[standard]>=0.30.0

# AWS Bedrock KB client
boto3>=1.34.0

# Data validation
pydantic>=2.4,<3.0

# .env support
python-dotenv>=1.0.1

# (Giúp tương thích type hints trên Python < 3.11)
typing-extensions>=4.8.0; python_version < "3.11"
